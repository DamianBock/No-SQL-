
<a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
  <form method="POST" action="/search">
    <input type="search" name="searchTerm" class="form-control" placeholder="Search..." aria-label="Search">
  </form>
</div>
<section class="pb-4 pt-4">
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

    <% if(food != '') { %>
      <% food.latest.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <% if (recipe.img && recipe.contenttype) { %>
            <img src="data:<%= recipe.contenttype %>;base64,<%= recipe.img.toString('base64') %>" loading="lazy">
          <% } %>

        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>

</section>

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Thai Recipes</h2>
    <a href="/kategorien/Thai" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof food.thai !== 'undefined' && food.thai.length > 0) { %>
      <% food.thai.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="data:<%= recipe.contenttype %>;base64,<%= recipe.img.toString('base64') %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Amerikanisch Recipes</h2>
    <a href="/kategorien/Amerikanisch" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(food.amerikanisch != '') { %>
      <% food.amerikanisch.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="data:<%= recipe.contenttype %>;base64,<%= recipe.img.toString('base64') %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Chinesisch Recipes</h2>
    <a href="/kategorien/Amerikanisch" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(food.chinesisch != '') { %>
      <% food.chinesisch.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="data:<%= recipe.contenttype %>;base64,<%= recipe.img.toString('base64') %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>
<a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
